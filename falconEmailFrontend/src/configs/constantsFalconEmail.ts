
const token = import.meta.env.VITE_FALCONEMAIL_API_TOKEN;
const falconEmailURLBaseSearch = import.meta.env.VITE_FALCONEMAIL_URL_BASE_SEARCH
const faconEmailIndex = import.meta.env.VITE_FALCONEMAIL_INDEX

export default {
    token,
    falconEmailURLBaseSearch,
    faconEmailIndex
}

        